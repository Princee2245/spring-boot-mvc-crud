spring.application.name=cruddemo

spring.datasource.url=jdbc:mysql://localhost:3306/student_tracker
spring.datasource.username=springstudent
spring.datasource.password=springstudent

#turn off the spring boot banner

spring.main.banner-mode=false;

#reduce logging level. set logging level to warn
logging.level.root=warn


#add logging configs to display sql statements
logging.level.org.hibernate.SQL=debug;
logging.level.org.hibernate.orm.jdbc.bind=trace

#to create table automatically by dropping and then recreating once again
#so we lose the data that was earlier stored
# spring.jpa.hibernate.ddl=create

# the update will keep the existing data in the table
# spring.jpa.hibernate.ddl=update
